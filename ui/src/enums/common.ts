export enum DeviceType {
  Mobile = 'Mobile',
  Desktop = 'Desktop'
}

export enum ValidType {
  Application = 'application',
  Dataset = 'dataset',
  User = 'user'
}

export enum ValidCount {
  Application = 1000,
  Dataset = 1000,
  User = 100
}
